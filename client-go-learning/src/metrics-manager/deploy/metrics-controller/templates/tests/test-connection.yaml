apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metrics-controller.fullname" . }}-test-connection"
  labels:
    {{- include "metrics-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metrics-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
